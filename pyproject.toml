[project]
name = "fdshield-ml"
version = "0.1.0"
description = "FDShield fraud-detection model comparison and MLflow integration"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "fastapi>=0.141,<1",
    "google-cloud-storage==3.13.0",
    "mlflow==3.13.0",
    "numpy>=2.4,<3",
    "pandas>=2.3,<4",
    "python-dotenv>=1.1,<2",
    "scikit-learn>=1.7,<2",
    "uvicorn>=0.52,<1",
    "xgboost>=3.1,<4",
]

[dependency-groups]
dev = [
    "httpx2>=2,<3",
    "pytest>=8.4,<10",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
